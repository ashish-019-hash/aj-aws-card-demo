**Important Note on Multiple Runs:** When using this prompt multiple times, each run is for analyzing a **DIFFERENT program** (e.g., PROG001, PROG002, PROG003, etc.), not an attempt to get a better response for the same program. Even if you are re-analyzing the same program, treat each run as an independent analysis. Always explicitly specify which program you are analyzing at the beginning of your response. Use the consistent extraction format as defined in this prompt, regardless of previous runs. Each program analysis is independent.
